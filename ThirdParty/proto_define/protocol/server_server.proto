syntax = "proto3";
package S2SPacket;

//message PacketBase
//{
    //repeated int32 all_dest_info = 1;
    //int32 owner_info = 2;
    //string msg_name = 3;
    //bytes msg_data = 4;
//}
//在发送的时候在msg头部加上数据长度

message PktRegisterReq
{
}
message PktRegisterAck
{
    int32 server_info = 1;
}
message PktUpdateServerInfo
{
    enum UpdateType
    {
        add = 0;
        sync = 1;
        remove = 2;
    }
    UpdateType type = 1;
    repeated int32 server_info = 2;
}